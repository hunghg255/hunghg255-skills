# SFX Library · huashu-design

> All generated by ElevenLabs Sound Generation API, Apple keynote-level audio quality.
> Production-grade SFX asset library, covering all scenarios for huashu animation/demos/product demos.

**Asset Location**: `assets/sfx/<category>/<name>.mp3`
**Total**: 37 SFX (30 batch generated + 7 v7b reserved)
**Generation Model**: ElevenLabs Sound Generation API (prompt_influence 0.4)
**Audio Quality**: 44.1kHz MP3, Apple keynote-level clarity, no additional reverb

---

## Directory Structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick Index

### ⌨️ Keyboard

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | Single keypress (mechanical keyboard single key) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | Continuous fast typing (demo input prompt) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | Backspace delete | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | Spacebar tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | Enter confirm (v7b reserved) | enter key press, crisp tactile |

### 🎯 UI

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | Standard UI click (v7b reserved) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | Soft UI click (secondary button/link) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | Element focus/selection (v7b reserved) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | Hover hint (subtle feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | Mobile tap (iOS interface) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | Toggle switch on | ios toggle switch flip, satisfying click |

### 🌊 Transition

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | Standard whoosh (v7b reserved) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | Fast whoosh (title flash, tab switch) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | Horizontal swipe (carousel, tab switch) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | Element slide-in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | Soft dissolve (image fade out/in) | soft dissolve, airy shimmer |

### 🃏 Container

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | Card snap/position (v7b reserved) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | Card flip (front/back switch) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | Stack collapse (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | Modal open | modal popping open, whoosh + thud |

### 🔔 Feedback

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | Success hint (payment success, task complete) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | Error hint (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | Message popup (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | Achievement unlock (milestone, badge) | triumphant rising arpeggio, game-style |

### ⏳ Progress

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | Loading tick (progress bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | Complete confirm (step done) | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI generation start | soft rising shimmer, magical whoosh |

### 💥 Impact

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | Logo impact (v7b reserved) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | Longer logo impact (cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | Stamp hit (certification, seal) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | Object drop (insertion, placement) | heavy thud, wood surface contact |

### ✨ Magic

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | Magic sparkle (AI highlight, surprise) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing sound (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | Transform transition (morph effect) | rising shimmer whoosh with sparkle tail |

### 💻 Terminal

| File | Duration | Use Case | Prompt Key Points |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | Command execution | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | Output appear | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | Cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended Combinations by Scenario

### 💻 Terminal Interaction Demo
```
type (0.5s) → enter (0.5s) → command-execute (0.5s) → output-appear (0.6s)
```
Loop element: `cursor-blink` as ambient sound during idle.

### 🃏 Card Selection Flow
```
hover-subtle (0.5s, UI hover) → click-soft (0.5s, click) → card-snap (0.5s, position)
```
Or advanced version: `card-flip` for front/back switch.

### 🤖 AI Generation Full Flow
```
generate-start (0.8s, start) → ai-process (1.2s, processing) → sparkle (0.8s, flash) → complete-done (0.8s, complete)
```
On error: use `error-tone` instead of `complete-done`.

### 🎬 Logo Reveal (Brand Moment)
```
whoosh-fast (0.6s, build-up) → logo-reveal-v2 (1.5s, impact) → sparkle (0.8s, tail)
```
Simplified: `whoosh → logo-reveal` (direct v7b two-piece set).

### 📱 UI Interaction Demo (Mobile)
```
tap-finger (0.5s, tap) → slide-in (0.6s, panel slide) → toggle-on (0.5s, switch)
```
On complete: `success-chime` or `notification-pop`.

### 📊 Data Visualization/Dashboard
```
loading-tick (0.5s, beat) × N → complete-done (0.8s, data ready) → achievement (1.5s, impressive finish)
```

### 🎯 Form Submission Flow
```
click-soft (0.5s) → loading-tick ×2 (1.0s) → success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### 🪄 Magic Transform Scenario
```
whoosh-fast (0.6s) → transform (1.0s) → sparkle (0.8s)
```
Suitable for: element morphing, before/after comparison, "AI rewrite" demos.

---

## Usage Guidelines

### Volume Recommendations (from apple-gallery-showcase.md audio dual-track system)
- **SFX Main Track**: `1.0` (no attenuation)
- **BGM Background Track**: `0.4 ~ 0.5` (SFX clearly audible)
- **Multiple SFX Overlay**: Use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg Concatenation Templates
```bash
# Single SFX aligned to timestamp:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection Decision Tree
1. **Has tactile action** (typing/clicking/swiping) → `keyboard/` or `ui/`
2. **Element enter/exit** → `transition/`
3. **Container layer operations** (card/modal) → `container/`
4. **Status feedback** (success/failure/notification) → `feedback/`
5. **Progress/time passage** → `progress/`
6. **Brand impact/important moments** → `impact/`
7. **AI magic/transform** → `magic/`
8. **Command line/code demo** → `terminal/`

### Avoid Sound Stacking
- Same timestamp: `max 2 SFX` concurrent
- When BGM drops below 0.3: can use 3
- Clear other SFX during brand impact (leave 0.2s gap before impact)

---

## Prompt Writing Principles (for Reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**Three Elements of Good Prompts**:
1. **Physical sound description**: What object, what action ("mechanical keyboard single key press")
2. **Texture/style constraint**: apple-style / ios-style / cinematic / retro
3. **Anti-example exclusion**: no reverb / clean studio / minimal

❌ "click sound"
✅ "crisp ui button click, clean modern interface sound, apple-style, high pitched"

❌ "magic"
✅ "bright twinkling stars sound, high pitched glittery chime, fairy dust"

---

## See Also
- Audio dual-track system and ffmpeg concatenation: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-time batch generator)
